if status is-interactive
    	neofetch
	alias stop='ps aux | grep  | sed "s/  */ /g" | cut -d " " -f 2 | sed ':a;N;$!ba;s/\n/ /g' | xargs kill'
	# Commands to run in interactive sessions can go here
end
